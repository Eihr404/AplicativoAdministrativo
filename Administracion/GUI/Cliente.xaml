<UserControl x:Class="Administracion.GUI.Cliente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Administracion.GUI"
        mc:Ignorable="d"
        Height="450" Width="800">
    <Grid Grid.Row="2">
        <TabControl SelectionChanged="TabControl_SelectionChanged">
            <TabItem Header="Gestión de clientes">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Barra superior -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBox x:Name="TxtBuscar" Width="260" Margin="0,0,10,0"
                             VerticalContentAlignment="Center"
                             ToolTip="Buscar por usuario o cédula"/>
                        <Button Content="Buscar" Width="90" Click="BtnBuscar_Click" Margin="0,0,10,0"/>
                        <Button Content="Refrescar" Width="90" Click="BtnRefrescar_Click" Margin="0,0,10,0"/>
                        <Button Content="Bloquear/Activar" Width="130" Click="BtnToggleEstado_Click" Margin="0,0,10,0"/>
                        <Button Content="Cambiar rol" Width="110" Click="BtnCambiarRol_Click" Margin="0,0,10,0"/>
                        <Button Content="Ingresar" Width="90" Click="CliBtnIngresar" />
                    </StackPanel>

                    <!-- Tabla -->
                    <DataGrid x:Name="GridClientes"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Usuario" Binding="{Binding UsrNombre}" Width="200"/>
                            <DataGridTextColumn Header="Cédula" Binding="{Binding CliCedula}" Width="140"/>
                            <DataGridTextColumn Header="Rol" Binding="{Binding Rol}" Width="160"/>
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <!-- Para panel -->
                    <Border x:Name="PanelNuevoCliente"
                            Grid.Row="2"
                            Margin="0,10,0,0"
                            Padding="10"
                            BorderBrush="Gray"
                            BorderThickness="1" 
                            Visibility="Collapsed">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Usuario"/>
                                <TextBox x:Name="TxtUsrNombre"/>

                                <TextBlock Text="Cédula" Margin="0,5,0,0"/>
                                <TextBox x:Name="TxtCedula"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Contraseña"/>
                                <TextBox x:Name="TxtPassword"/>

                                <TextBlock Text="Rol" Margin="0,5,0,0"/>
                                <ComboBox x:Name="CmbRol">
                                    <ComboBoxItem Content="CLIENTE"/>
                                    <ComboBoxItem Content="ADMIN"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
                                <Button Content="Guardar" Width="90" Click="BtnGuardar_Click"/>
                                <Button Content="Cancelar" Width="90" Margin="10,0,0,0"
                    Click="BtnCancelar_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>

</UserControl>
