<UserControl x:Class="Administracion.GUI.Cliente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Administracion.GUI"
        mc:Ignorable="d"
        Height="450" Width="863">
    <Grid Grid.Row="2" Margin="10">        
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Barra superior -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="TxtBuscar" Width="260" Margin="0,0,10,0"
              VerticalContentAlignment="Center"
              ToolTip="Buscar por usuario o cédula"/>
            <Button Content="Consultar" Width="90" Click="BtnConsultar_Click" Margin="0,0,10,0"/>
            <Button Content="Ingresar" Width="90" Click="CliBtnIngresar" Margin="0,0,10,0"/>
            <Button Content="Eliminar" Width="90" Click="BtnToggleEstado_Click" Margin="0,0,10,0"/>
            <Button Content="Modificar rol" Width="90" Click="BtnModificarRol_Click" Margin="0,0,10,0"/>
        </StackPanel>

            <!-- Tabla -->
            <DataGrid x:Name="GridClientes"
               Grid.Row="1"
               AutoGenerateColumns="False"
               IsReadOnly="True"
               SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Usuario" Binding="{Binding UsrNombre}" Width="200"/>
                    <DataGridTextColumn Header="Cédula" Binding="{Binding CliCedula}" Width="140"/>
                    <DataGridTextColumn Header="Rol" Binding="{Binding Rol}" Width="160"/>
                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Para panel -->
        <Border x:Name="PanelNuevoCliente"
        Grid.Row="2"
        Margin="0,10,0,0"
        Padding="15"
        BorderBrush="#CCC"
        BorderThickness="1"
        CornerRadius="6"
        Background="#FAFAFA"
        Visibility="Collapsed">

            <StackPanel>

                <!-- TÍTULO -->
                <TextBlock Text="REGISTRO DE CLIENTE"
                   FontSize="15"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

                <!-- USUARIO -->
                <Label Content="Usuario:"/>
                <TextBox x:Name="TxtUsrNombre" Margin="0,0,0,10"/>

                <!-- CÉDULA -->
                <Label Content="Cédula:"/>
                <TextBox x:Name="TxtCedula" Margin="0,0,0,10"/>

                <!-- CONTRASEÑA -->
                <Label Content="Contraseña:"/>
                <PasswordBox x:Name="TxtPassword" Margin="0,0,0,10"/>

                <!-- ROL -->
                <Label Content="Rol:"/>
                <ComboBox x:Name="CmbRol" Margin="0,0,0,15">
                    <ComboBoxItem Content="CLIENTE"/>
                    <ComboBoxItem Content="ADMIN"/>
                </ComboBox>

                <!-- BOTONES -->
                <UniformGrid Columns="2">
                    <Button Content="Guardar"
                    Margin="5"
                    Click="BtnGuardar_Click"
                    Background="#DFF0D8"/>

                    <Button Content="Cancelar"
                    Margin="5"
                    Click="BtnCancelar_Click"/>
                </UniformGrid>

            </StackPanel>
        </Border>

    </Grid>

</UserControl>
