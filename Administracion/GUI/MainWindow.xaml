<Window x:Class="Administracion.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:gui="clr-namespace:Administracion.GUI"
        xmlns:local="clr-namespace:Administracion"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="1080">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- MENÚ -->
        <Menu Grid.Row="0">
            <MenuItem Header="Gestión de proveedores"
                      Tag="Proveedores"
                      Click="Navigation_Click"/>
            <MenuItem Header="Gestión de productos"
                      Tag="Productos"
                      Click="Navigation_Click"/>
            <MenuItem Header="Gestión de clientes"
                      Tag="Clientes"
                      Click="Navigation_Click"/>
        </Menu>

        <!-- CONTENEDOR DE PESTAÑAS -->
        <ContentControl x:Name="MainContent"
                        Grid.Row="2"/>
    </Grid>
</Window>
