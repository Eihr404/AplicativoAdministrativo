<UserControl x:Class="Administracion.GUI.Proveedor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Administracion.GUI"
        mc:Ignorable="d"
        d:DesignHeight="600" d:DesignWidth="800">
    <Grid Grid.Row="3" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Barra superior -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Center" Grid.Row="0">
            <StackPanel>
                <Label Content="Ingrese el codigo del proveedor" FontSize="12" FontWeight="Bold"          HorizontalAlignment="Center"/>
                <TextBox x:Name="TxtBuscar" Width="260" Margin="0,0,10,0"  VerticalContentAlignment="Center" ToolTip="Buscar por cédula"/>
            </StackPanel>
                
            <Button Content="Consultar" Width="90" Click="BtnConsultar_Click" Margin="0,0,10,0"/>
            <Button Content="Ingresar" Width="90" Click="CliBtnIngresar" Margin="0,0,10,0"/>
            <Button Content="Eliminar" Width="90" Click="BtnEliminar_Click" Margin="0,0,10,0"/>
            <Button Content="Modificar" Width="90" Click="BtnModificar_Click" Margin="0,0,10,0"/>
        </StackPanel>

        <!-- Tabla -->
        <DataGrid x:Name="GridProveedor"
               Grid.Row="2"
               AutoGenerateColumns="False"
               IsReadOnly="True"
               SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="ID"
                    Binding="{Binding PrvCodigo}"
                    Width="Auto"/>

                <DataGridTextColumn
                    Header="Nombre"
                    Binding="{Binding PrvNombre}"
                    Width="*"/>

                <DataGridTextColumn
                    Header="RUC"
                    Binding="{Binding EmpCedulaRuc}"
                    Width="*"/>

                <DataGridTextColumn
                    Header="Teléfono"
                    Binding="{Binding PrvTelefono}"
                    Width="*"/>

            </DataGrid.Columns>

        </DataGrid>

        <!-- Para panel -->
        <Border x:Name="PanelNuevoProveedor"
        Grid.Row="2"
        Margin="0,10,0,0"
        Padding="15"
        BorderBrush="#CCC"
        BorderThickness="1"
        CornerRadius="6"
        Background="#FAFAFA"
        Visibility="Collapsed">

            <StackPanel>

                <!-- TÍTULO -->
                <TextBlock Text="REGISTRO DE PROVEEDOR"
                   FontSize="15"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

                <!-- Nombre -->
                <Label Content="Nombre:"/>
                <TextBox x:Name="TxtPrvNombre" Margin="0,0,0,10"/>

                <!-- CÉDULA -->
                <Label Content="Cédula:"/>
                <TextBox x:Name="TxtPrvCedula" Margin="0,0,0,10"/>

                <!-- Telefono -->
                <Label Content="Telefono:"/>
                <TextBox x:Name="TxtPrvTelefono" Margin="0,0,0,10"/>

                <!-- Direccion -->
                <Label Content="Direccion:"/>
                <TextBox x:Name="TxtPrvDireccion" Margin="0,0,0,10"/>


                <!-- BOTONES -->
                <UniformGrid Columns="2">
                    <Button Content="Guardar"
                    Margin="5"
                    Click="BtnGuardar_Click"
                    Background="#DFF0D8"/>

                    <Button Content="Cancelar"
                    Margin="5"
                    Click="BtnCancelar_Click"/>
                </UniformGrid>

            </StackPanel>
        </Border>

    </Grid>

</UserControl>
