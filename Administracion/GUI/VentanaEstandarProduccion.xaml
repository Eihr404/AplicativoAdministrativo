<UserControl x:Class="Administracion.GUI.EstandarProduccion"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid x:Name="edpGrid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="edpTxtblBuscarCodigo" Width="250" VerticalContentAlignment="Center"/>
            <Button x:Name="edpBtnConsultar" Content="Consultar" Width="90" Margin="5,0,0,0" Click="edpBtnConsultar_Click"/>
            <Button x:Name="edpBtnIngresar" Content="Ingresar" Width="90" Margin="5,0,0,0" Click="edpBtnIngresar_Click"/>
            <Button x:Name="edpBtnModificar" Content="Modificar" Width="90" Margin="5,0,0,0" Click="edpBtnModificar_Click"/>
            <Button x:Name="edpBtnEliminar" Content="Eliminar" Width="90" Margin="5,0,0,0" Click="edpBtnEliminar_Click"/>
        </StackPanel>

        <DataGrid x:Name="edpDatGri" 
                  Grid.Row="1" 
                  AutoGenerateColumns="False" 
                  SelectionMode="Single" 
                  IsReadOnly="True" 
                  MinHeight="200">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Cód. Materia Prima" Binding="{Binding MtpCodigo}" Width="Auto"/>
                <DataGridTextColumn Header="Cód. Producto" Binding="{Binding ProCodigo}" Width="Auto"/>
                <DataGridTextColumn Header="Descripción" Binding="{Binding EdpDescripcion}" Width="*"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding EdpCantidad}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border x:Name="PanelFormularioEdp"
                Grid.Row="2"
                Margin="0,10,0,0"
                Padding="15"
                BorderBrush="#CCC"
                BorderThickness="1"
                CornerRadius="6"
                Background="#FAFAFA"
                Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="DETALLE ESTÁNDAR DE PRODUCCIÓN" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Column="0" Margin="5">
                        <Label Content="Materia Prima:"/>
                        <ComboBox x:Name="cmbMateriaPrima" DisplayMemberPath="MtpNombre" SelectedValuePath="MtpCodigo"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5">
                        <Label Content="Producto:"/>
                        <ComboBox x:Name="cmbProducto" DisplayMemberPath="Nombre" SelectedValuePath="Codigo"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="5">
                        <Label Content="Descripción Estándar:"/>
                        <TextBox x:Name="txtEdpDescripcion" Height="40" TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="5">
                        <Label Content="Cantidad Necesaria:"/>
                        <TextBox x:Name="txtEdpCantidad"/>
                    </StackPanel>
                </Grid>

                <UniformGrid Columns="2" Margin="0,15,0,0">
                    <Button Content="Guardar" Margin="5" Click="BtnGuardar_Click" Background="#DFF0D8"/>
                    <Button Content="Cancelar" Margin="5" Click="BtnCancelar_Click"/>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>